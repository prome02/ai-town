# AI Town Docker 容器環境配置
# 這個配置會被複製到容器內

# Convex 本地後端配置
VITE_CONVEX_URL=http://127.0.0.1:3210
CONVEX_DEPLOYMENT=local

# 應用程式環境
NODE_ENV=production

# LLM API 配置（使用 host.docker.internal 訪問宿主機的 Ollama）
LLM_API_URL=http://host.docker.internal:11434

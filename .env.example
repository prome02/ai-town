# AI Town 本地環境配置
VITE_CONVEX_URL=http://127.0.0.1:3210

# 本地 Convex 部署配置
CONVEX_DEPLOYMENT=local

# LLM API 配置（本地開發）
LLM_API_URL=http://127.0.0.1:11434

# LLM 模型配置
LLM_CHAT_MODEL=glm-4.6:cloud
LLM_EMBEDDING_MODEL=nomic-embed-text
LLM_EMBEDDING_DIMENSION=768

# LLM 對話語言設定
# 支援的語言代碼: en (英文), zh-TW (繁體中文), zh-CN (簡體中文),
#                  ja (日文), ko (韓文), es (西班牙文), fr (法文), de (德文)
# 預設: en
LLM_CONVERSATION_LANGUAGE=en

# 其他環境變量
# 注意: Vite 不支援在 .env 中設定 NODE_ENV，請勿設定此變數
